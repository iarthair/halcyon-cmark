#ifndef _header_h
#define _header_h

int read_metadata_key (const char *s, char const **es, char buf[], int buflen);
int read_metadata_value (const char *s, char const **es, char buf[], int buflen);

#endif
